This is a simple weather api
They country names are abbreviated
The event.preventDefault() function helps the <form> not to redirect but still does exact action
API used is https://home.openweathermap.org/
